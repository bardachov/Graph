/**
 * @todo: что происходить во время создания примитива (запуск кода)
 */

/**
 * Примитивные тип данных(): string, number, boolean, null, undefined, symbol. Может хранить только простое значение
 * Особеный тип Object. Сложные структуры данных.
 * * Array - особая структура данных(подвид обьектов) для хранения упорядоченых коллекций.
 * * * очистка массива arr.length = 0
 * * * length = последний индекс + 1
 * 
 * 
 */